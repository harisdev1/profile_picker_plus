name: profile_picker_plus_example
description: Full example app for profile_picker_plus — showcasing every feature and configuration.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  # Reference the local package
  profile_picker_plus:
    path: ../
  image_cropper: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

/// The type of action available in the picker sheet or dialog.
enum OptionType {
  /// Pick an image from the device gallery.
  gallery,

  /// Capture a new image with the camera.
  camera,

  /// Remove the current profile photo.
  remove,
}

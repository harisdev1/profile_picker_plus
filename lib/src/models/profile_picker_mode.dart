/// Controls how the image source picker is presented to the user.
enum ProfilePickerMode {
  /// Present source selection as a modal bottom sheet (default).
  bottomSheet,

  /// Present source selection as a centered dialog.
  dialog,
}
